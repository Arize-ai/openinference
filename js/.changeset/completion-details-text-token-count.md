---
"@arizeai/openinference-semantic-conventions": patch
---

Add `LLM_TOKEN_COUNT_COMPLETION_DETAILS_TEXT` (`llm.token_count.completion_details.text`), the number of text tokens in the completion, alongside the existing `completion_details.reasoning` and `completion_details.audio` token counts.
