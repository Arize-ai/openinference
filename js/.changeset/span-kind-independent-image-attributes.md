---
"@arizeai/openinference-semantic-conventions": minor
---

Add `input.images` and `output.images` semantic conventions, letting any span kind record images without the LLM message structure, plus `image.mime_type` for images whose URL does not carry the type inline.
