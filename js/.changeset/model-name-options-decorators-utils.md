---
"@arizeai/openinference-core": minor
---

Add optional `requestModelName` and `responseModelName` options to `withSpan`, the `@observe` decorator, the `trace*` wrappers, and `getLLMAttributes`, emitting the `llm.request.model_name` and `llm.response.model_name` semantic conventions
