-r test-requirements.txt

# Resolve the newest compatible modern pair directly instead of upgrading over the
# legacy environment and relying on command ordering.
pydantic-ai>=2.24.0
genai-prices>=0.1.0
